/*Обратите внимание, что здесь мы не создаем, а получаем
 ранее созданный модуль (angular.module('sortApp')),
 так как метод module использует только один параметр.*/
var sortApp = angular.module('myApp', []);